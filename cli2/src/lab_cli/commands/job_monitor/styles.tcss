Horizontal {
  height: 100%;
}

ListView {
  width: 100%;
  border: solid green;
  display: none;
}

ListView.loaded {
  display: block;
}

JobDetails {
  width: 75%;
  border: solid blue;
  padding: 0 2;
  height: 100%;
  /* Ensure children stack vertically */
  layout: vertical;
}

/* --- New Styles for Layout --- */

/* The top details panel */
#job-info-container {
  height: 1fr;
  margin-bottom: 1;
}

/* The bottom artifacts panel */
#job-artifacts-container {
  height: 1fr;
  border-top: solid $primary-muted;
  background: $surface-lighten-1;
  padding: 1;
  display: none;
  /* Hidden until job selected */
}

#job-artifacts-container.visible {
  display: block;
}

#loading {
  width: 100%;
  height: 100%;
  content-align: center middle;
}

#job-buttons {
  height: auto;
  /* Removed 'dock: bottom' so artifacts can sit below it */
  margin-top: 1;
  padding: 0 0;
  display: none;
  background: $surface;
}

#job-buttons.visible {
  display: block;
}

#job-buttons Button {
  margin-right: 1;
  border: solid $primary-muted;
}

/* Modal Styles */
JobJsonModal {
  align: center middle;
}

#json-modal-container {
  width: 95%;
  height: 95%;
  border: thick $primary;
  background: $surface;
  padding: 1 2;
}

#json-modal-container TextArea {
  height: 1fr;
  margin-bottom: 1;
  border: none;
}

#btn-close-modal {
  width: 100%;
  dock: bottom;
}