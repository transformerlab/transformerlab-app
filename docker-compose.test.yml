services:
  transformerlab-test:
    build:
      context: .
      dockerfile: test/docker/Dockerfile
    ports:
      - "8338:8338"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8338"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
